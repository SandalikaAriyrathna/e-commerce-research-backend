# e-commerce-research-backend

<!-- commands -->

```
pip install fastapi uvicorn
python -m venv venv
venv\Scripts\activate 
pip install -r requirements.txt --default-timeout=100 
uvicorn main:app --reload

```

